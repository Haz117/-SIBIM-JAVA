<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import org.kordamp.ikonli.javafx.FontIcon?>

<VBox xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.sibim.controller.MainController"
      prefWidth="1300" prefHeight="820">

    <!-- ── Main row: sidebar + content ──
         The sidebar is layered ON TOP of the content row (via the outer
         StackPane) rather than laid out beside it as a plain HBox sibling.
         The active nav button gets translateX'd a few px to the right (see
         MainController#animateActivePill) so it "invades" the content area —
         translateX doesn't affect layout, only paint position, so it needs
         the sidebar drawn above the content row to not get clipped by it.
         The base HBox below only exists to reserve the 220px of horizontal
         space with a transparent spacer. -->
    <StackPane fx:id="outerStack" VBox.vgrow="ALWAYS">
        <HBox>
            <Region prefWidth="220" minWidth="220" maxWidth="220" styleClass="sidebar-backdrop"/>
            <StackPane fx:id="contentArea" HBox.hgrow="ALWAYS" styleClass="content-area"/>
        </HBox>

        <!-- ── Sidebar (drawn above the content row) ──
             Split into three parts: logo+user card pinned at top, the nav
             sections scrolling in the middle if the window is short, and
             the logout button + version pinned at the bottom — so the
             logout button can never be pushed off-screen by overflowing
             nav content the way it could when everything was one plain
             VBox with no scroll. -->
        <VBox styleClass="sidebar" prefWidth="220" minWidth="220" maxWidth="220" spacing="0"
              StackPane.alignment="TOP_LEFT">

            <!-- Logo -->
            <HBox spacing="10" alignment="CENTER_LEFT" styleClass="sidebar-logo-area">
                <padding><Insets top="18" right="18" bottom="0" left="10"/></padding>
                <Label text="S" styleClass="sidebar-logo-badge"/>
                <VBox spacing="0">
                    <Label text="SIBIM" styleClass="sidebar-logo"/>
                    <Label text="Bienes Municipales" styleClass="sidebar-app-sub"/>
                </VBox>
            </HBox>

            <Region prefHeight="14"/>

            <!-- User card -->
            <HBox spacing="11" alignment="CENTER_LEFT" styleClass="sidebar-user-box">
                <padding><Insets top="0" right="18" bottom="0" left="10"/></padding>
                <StackPane styleClass="user-avatar">
                    <Label fx:id="userAvatarLabel" text="U" styleClass="user-avatar-text"/>
                </StackPane>
                <VBox spacing="4" HBox.hgrow="ALWAYS">
                    <Label fx:id="userNameLabel" styleClass="sidebar-username" wrapText="true"/>
                    <Label fx:id="userRolLabel" styleClass="sidebar-userrole"/>
                </VBox>
            </HBox>

            <Region prefHeight="10"/>
            <Separator styleClass="sidebar-divider">
                <padding><Insets left="10" right="18"/></padding>
            </Separator>

            <!-- Scrollable nav sections -->
            <ScrollPane fitToWidth="true" VBox.vgrow="ALWAYS" styleClass="sidebar-scroll">
                <VBox spacing="3">
                    <padding><Insets top="6" right="18" bottom="6" left="10"/></padding>

                    <Label text="NAVEGACIÓN" styleClass="nav-section-label"/>

                    <Button fx:id="btnDashboard" text="Dashboard" onAction="#onDashboard" styleClass="nav-btn" maxWidth="Infinity" contentDisplay="LEFT" graphicTextGap="10">
                        <graphic><FontIcon iconLiteral="mdi2v-view-dashboard" styleClass="nav-icon"/></graphic>
                        <tooltip><Tooltip text="Dashboard  (Ctrl+1)"/></tooltip>
                    </Button>
                    <Button fx:id="btnOrganigrama" text="Organigrama" onAction="#onOrganigrama" styleClass="nav-btn" maxWidth="Infinity" contentDisplay="LEFT" graphicTextGap="10">
                        <graphic><FontIcon iconLiteral="mdi2s-sitemap" styleClass="nav-icon"/></graphic>
                        <tooltip><Tooltip text="Organigrama  (Ctrl+2)"/></tooltip>
                    </Button>
                    <Button fx:id="btnProductos" text="Bienes" onAction="#onProductos" styleClass="nav-btn" maxWidth="Infinity" contentDisplay="LEFT" graphicTextGap="10">
                        <graphic><FontIcon iconLiteral="mdi2p-package-variant" styleClass="nav-icon"/></graphic>
                        <tooltip><Tooltip text="Inventario de bienes  (Ctrl+3)"/></tooltip>
                    </Button>
                    <Button fx:id="btnCategorias" text="Categorías" onAction="#onCategorias" styleClass="nav-btn" maxWidth="Infinity" contentDisplay="LEFT" graphicTextGap="10">
                        <graphic><FontIcon iconLiteral="mdi2t-tag-multiple" styleClass="nav-icon"/></graphic>
                        <tooltip><Tooltip text="Categorías de bienes  (Ctrl+4)"/></tooltip>
                    </Button>

                    <Region prefHeight="4"/>
                    <Separator styleClass="sidebar-divider"/>
                    <Region prefHeight="4"/>
                    <Label text="OPERACIONES" styleClass="nav-section-label"/>

                    <Button fx:id="btnMovimientos" text="Movimientos" onAction="#onMovimientos" styleClass="nav-btn" maxWidth="Infinity" contentDisplay="LEFT" graphicTextGap="10">
                        <graphic><FontIcon iconLiteral="mdi2s-swap-vertical" styleClass="nav-icon"/></graphic>
                        <tooltip><Tooltip text="Historial de movimientos  (Ctrl+5)"/></tooltip>
                    </Button>

                    <!-- Alertas con badge -->
                    <StackPane maxWidth="Infinity" alignment="CENTER_RIGHT">
                        <Button fx:id="btnAlertas" text="Alertas" onAction="#onAlertas"
                                styleClass="nav-btn" maxWidth="Infinity" StackPane.alignment="CENTER"
                                contentDisplay="LEFT" graphicTextGap="10">
                            <graphic><FontIcon iconLiteral="mdi2b-bell-alert-outline" styleClass="nav-icon"/></graphic>
                            <tooltip><Tooltip text="Alertas de inventario  (Ctrl+6)"/></tooltip>
                        </Button>
                        <Label fx:id="alertBadge" text="0"
                               styleClass="alert-nav-badge"
                               visible="false" managed="false"
                               StackPane.alignment="CENTER_RIGHT"/>
                    </StackPane>

                    <Button fx:id="btnReportes" text="Reportes" onAction="#onReportes" styleClass="nav-btn" maxWidth="Infinity" contentDisplay="LEFT" graphicTextGap="10">
                        <graphic><FontIcon iconLiteral="mdi2f-file-chart-outline" styleClass="nav-icon"/></graphic>
                        <tooltip><Tooltip text="Exportar reportes  (Ctrl+7)"/></tooltip>
                    </Button>
                    <Button fx:id="btnDepreciacion" text="Depreciación" onAction="#onDepreciacion" styleClass="nav-btn" maxWidth="Infinity" contentDisplay="LEFT" graphicTextGap="10">
                        <graphic><FontIcon iconLiteral="mdi2c-chart-line" styleClass="nav-icon"/></graphic>
                        <tooltip><Tooltip text="Depreciación de activos  (Ctrl+9)"/></tooltip>
                    </Button>

                    <Region prefHeight="4"/>
                    <Separator styleClass="sidebar-divider"/>
                    <Region prefHeight="4"/>
                    <Label text="SISTEMA" styleClass="nav-section-label"/>

                    <Button fx:id="btnConfiguracion" text="Configuración" onAction="#onConfiguracion" styleClass="nav-btn" maxWidth="Infinity" contentDisplay="LEFT" graphicTextGap="10">
                        <graphic><FontIcon iconLiteral="mdi2c-cog-outline" styleClass="nav-icon"/></graphic>
                        <tooltip><Tooltip text="Configuración  (Ctrl+8)"/></tooltip>
                    </Button>
                </VBox>
            </ScrollPane>

            <!-- Pinned footer: always visible regardless of nav list length -->
            <Separator styleClass="sidebar-divider">
                <padding><Insets left="10" right="18"/></padding>
            </Separator>
            <VBox spacing="4">
                <padding><Insets top="6" right="18" bottom="14" left="10"/></padding>
                <Button text="Cerrar sesión" onAction="#onLogout" styleClass="logout-btn" maxWidth="Infinity" contentDisplay="LEFT" graphicTextGap="10">
                    <graphic><FontIcon iconLiteral="mdi2l-logout" styleClass="nav-icon"/></graphic>
                    <tooltip><Tooltip text="Cerrar sesión actual"/></tooltip>
                </Button>
                <Button text="Acerca de" onAction="#onAcercaDe" styleClass="about-btn" maxWidth="Infinity" contentDisplay="LEFT" graphicTextGap="10">
                    <graphic><FontIcon iconLiteral="mdi2i-information-outline" styleClass="nav-icon"/></graphic>
                    <tooltip><Tooltip text="Versión e información del sistema"/></tooltip>
                </Button>
                <Region prefHeight="4"/>
                <Label text="SIBIM v1.0 · 2026" styleClass="sidebar-version" maxWidth="Infinity"/>
            </VBox>
        </VBox>
    </StackPane>

    <!-- ── Status bar ── -->
    <HBox styleClass="status-bar" alignment="CENTER_LEFT" spacing="16">
        <padding><Insets top="5" right="16" bottom="5" left="16"/></padding>
        <Label fx:id="statusDbLabel" text="⬤  Conectado" styleClass="status-dot-ok"/>
        <Separator orientation="VERTICAL" styleClass="status-separator"/>
        <Label fx:id="statusUserLabel" text="—" styleClass="status-text"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Label text="H. Ayuntamiento de Ixmiquilpan" styleClass="status-text"/>
        <Separator orientation="VERTICAL" styleClass="status-separator"/>
        <Label text="⌨ Atajos (F1)" styleClass="status-shortcut-hint" onMouseClicked="#onShowShortcuts">
            <tooltip><Tooltip text="Ver todos los atajos de teclado"/></tooltip>
        </Label>
        <Separator orientation="VERTICAL" styleClass="status-separator"/>
        <Label fx:id="statusTimeLabel" text="00:00" styleClass="status-time"/>
    </HBox>
</VBox>
